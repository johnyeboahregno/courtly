[mysqld]
# Tuned for the 3.7GB / 2 vCore OVH VPS this stack actually targets (51.38.64.121) —
# app/nginx/queue/caddy all share this box too, so MySQL gets a modest slice.
innodb_buffer_pool_size = 768M
innodb_log_file_size = 128M
innodb_flush_log_at_trx_commit = 2
max_connections = 50
